feat: complete containerization and configuration management

- Add full Docker stack (API, PostgreSQL, Redis) with health checks
- Externalize all sensitive config to .env (ports, credentials, API keys)
- Create .env.example template with all configuration variables
- Add Docker commands to Makefile (docker-up, docker-down, docker-build, etc.)
- Fix port conflicts (PostgreSQL: 5433, Redis: 6380 by default)
- Auto-detect Docker Compose version (V1/V2 compatibility)

- Remove duplicate model definitions and unnecessary files
- Clean up code inconsistencies (imports, docstrings)
- Update .gitignore for temporary files and logs

All services now run in Docker with environment-based configuration.
